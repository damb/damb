#!/usr/bin/env bash

SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

. "${SCRIPT_DIR}/dev/dev_alias"
. "${SCRIPT_DIR}/dev2/dev_alias"

# XXX(damb): this file is optionally written by `customize.sh`.
test -f ${HOME}/.bashrc_dev && . ${HOME}/.bashrc_dev

